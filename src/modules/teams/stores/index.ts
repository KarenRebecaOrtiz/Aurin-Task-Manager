/**
 * @deprecated Teams are now managed through the unified dataStore.
 * Import from '@/stores/dataStore' instead:
 *
 * import { useDataStore, useTeams, useTeamsLoading, type Team } from '@/stores/dataStore';
 *
 * Legacy exports kept for backwards compatibility during migration.
 */
export * from './teamsStore';
